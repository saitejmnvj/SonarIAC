---
 - hosts: node1
   become: true
   tasks: 
    - name: Updating apt
      yum:
       update_cache=yes
    
    - name: Install wget
      yum: pkg='wget' state=installed
      
    - name: Install JDK 8
      yum: pkg='openjdk-8-jdk' state=installed
      
    - name: Install MySQL
      yum: pkg='mysql-server' state=installed
      
